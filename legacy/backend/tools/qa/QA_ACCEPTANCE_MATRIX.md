# QA Acceptance Matrix

This matrix is derived from:

- [canonical_runtime_reconcile.md](C:/Users/tmdtk/Xignal/legacy/docs/canonical_runtime_reconcile.md)

## Acceptance Table

| Scenario | Invariant | Input Conditions | Expected Ledger | Expected Snapshot | Expected Grid/Signal Row | Expected Reservation | Expected Audit/msg | PASS Criteria |
|---|---|---|---|---|---|---|---|---|
| same PID duplicate grid entry | same PID + same fill unit = one ledger application | same PID, same clientOrderId/orderId/tradeId or same time/qty/price, eventType differs | one entry fill only | openQty increases once | grid qty does not double | no duplicate reservation side effect | duplicate ignored audit | one ledger entry, one qty increase, duplicate audit present |
| same PID duplicate signal entry | same PID + same fill unit = one ledger application | same PID, same signal entry fill, eventType differs | one entry fill only | openQty increases once | signal runtime qty reflects single fill | no duplicate reservation side effect | duplicate ignored audit | one ledger entry, no double qty |
| duplicate exit | same PID + same fill unit = one ledger application | same runtime exit and reconciled exit fill | one exit application only | openQty not negative | signal/grid row not over-closed | reservation not double-filled | duplicate ignored audit | one close application, pnl not doubled |
| different PID same symbol/side | same symbol/side but different PID means different ledgers | same symbol/side, different PID, different order ids | two independent ledgers | two independent snapshots | independent rows | independent reservations | separate audit traces | no cross-PID dedupe |
| partial fill under same orderId with distinct tradeIds | orderId alone is not fill identity | same orderId, different tradeIds | one row per tradeId | openQty accumulates partials | runtime qty reflects sum | reservations stay consistent | fill-level audit | each partial fill preserved |
| signal market entry recovery with partial fills | same PID + same exchange fill unit = one ledger application; partial fill under same orderId with distinct tradeIds remains distinct; snapshot is projection; protection sync follows recovered ledger/snapshot state | signal PID, market order filled on exchange, WebSocket/runtime event missing or not persisted, REST allOrders/userTrades has 2 partial fills, local ledger/snapshot/reservation missing before recovery | REST recovery creates 2 entry rows and preserves each `sourceTradeId` | snapshot OPEN with cumulative `openQtyAfter` and weighted avg entry | signal row `r_qty/r_exactPrice/r_exactTime` synced to recovered open state | protection sync path reached from recovered state | recovery attempt/found-trades/apply/snapshot/protection trace plus duplicate ignored on re-observation | both fills recovered exactly once, snapshot OPEN, exact fields synced, protection sync path reached, duplicate replay ignored |
| signal split TP multi-fill exit accounting | same PID + same exchange fill unit = one ledger application; partial fill / split TP are separate fill units; snapshot is projection | signal PID, openQty 14074, split TP produces 2 Binance fills under split TP clientOrderId family, local exit ledger missing or only partial before recovery | REST recovery creates 2 exit rows and preserves each `sourceTradeId`; `openQtyAfter` steps `14074 -> 7037 -> 0` | snapshot CLOSED with cumulative realized pnl | signal row synced flat after full recovery | correction flatten not needed in normal recovery | exit fill-unit recovery/apply trace plus duplicate ignored on replay | two exit rows, no fill collapse, no flatten replacement, duplicate replay ignored |
| signal TIME exit fill recovery | TIME close is a signal exit path; same PID + same exchange fill unit = one ledger application; aggregate exposure from other PIDs must not block owner recovery | signal PID SHORT openQty=36, `TIME_<uid>_<pid>_<entryOrderId>` market close is FILLED on Binance, same symbol/side aggregate is still non-flat due other PIDs, local exit ledger missing | one `EXCHANGE_RECONCILED_EXIT_FILL` row with `sourceClientOrderId`, `sourceOrderId`, and `sourceTradeId` preserved | full fill closes snapshot to CLOSED/openQty=0 | signal row converges to READY/r_qty=0 | MARKET_TIME reservation becomes FILLED; actual fill recovery is not replaced by correction | `SIGNAL_TIME_EXIT_RECOVERY_*` and duplicate ignored audit | TIME fill is recovered before aggregate-flat fallback; duplicate replay does not double ledger/PnL |
| signal TIME exit sibling protection cleanup | TIME full close must not silently hide active sibling TP/STOP | signal PID has active TP/STOP plus TIME close full fill; Binance still reports sibling TP/STOP active | recovered TIME exit row only | snapshot CLOSED/openQty=0 | signal row READY/r_qty=0 and no re-register | sibling TP/STOP are not silently marked canceled without Binance evidence; active sibling protection is surfaced as risk | `SIGNAL_TIME_EXIT_SIBLING_PROTECTION_ACTIVE` / USER_ACTION_REQUIRED audit | no `BOUND_REGISTERED` after full TIME close; live-readonly risk flags protection without PID-owned openQty |
| signal TIME exit partial fill | partial TIME close reduces only the filled quantity | signal PID SHORT openQty=36, TIME close PARTIALLY_FILLED qty=20, local exit ledger missing | one exit row qty=20 with `sourceTradeId` preserved | snapshot remains OPEN/openQty=16 | signal row remains EXACT/r_qty=16 | MARKET_TIME reservation becomes PARTIAL; oversized sibling protection must be reported if not adjusted | `SIGNAL_TIME_EXIT_RECOVERY_APPLY_FILL` audit | remaining qty is not over-closed and no correction flatten replaces the partial fill |
| split TP / partial close | distinct exit fill units remain distinct | one entry, multiple exit fill units | one row per exit fill | openQty decreases stepwise | runtime qty decreases stepwise | reservation fill progresses correctly | exit fill audit per unit | no collapse and no double count |
| grid multi-trade entry preservation | same PID + same exchange fill unit = one ledger application; partial fill / multi-trade entry fill units remain distinct | grid PID, one entry order, same orderId has multiple distinct tradeIds, including two fills with identical qty/time/price but different `sourceTradeId` | one entry row per `sourceTradeId` | snapshot OPEN with accumulated qty and weighted avg entry | grid leg qty reflects sum | reservations stay unchanged until protection phase | grid fill-unit recovery/apply trace | all tradeIds are preserved, identical qty/time/price fills are not collapsed, duplicate replay ignored |
| grid multi-trade exit preservation | same PID + same exchange fill unit = one ledger application; partial fill / multi-trade exit fill units remain distinct | grid PID, one exit order, same orderId has 2 distinct tradeIds | 2 exit rows with preserved `sourceTradeId` and summed realized pnl | snapshot reduced/closed exactly once | grid leg qty converges without correction replacing missing fills | reservation lifecycle remains auditable | grid fill-unit recovery/apply trace | two exit rows, no missing fill unit replaced by correction, duplicate replay ignored |
| same order/client distinct sourceTradeId late recovery | sourceTradeId is the fill identity when present | a late fill has the same orderId/clientOrderId/qty/price/time as an existing fill but a new `sourceTradeId` | new tradeId is inserted as a separate ledger row | snapshot openQty accumulates both fills | row qty reflects both fills | no new reservation side effect | fill identity audit / duplicate ignored only for exact tradeId repeats | no collapse by orderId/clientOrderId fallback |
| current incident XRP residual 3.7 | aggregate exposure from Binance must not be hidden by local projection | Binance XRP LONG exceeds local PID-owned LONG by residual 3.7 after missed grid fill | no writes | no writes | no writes | no writes | live-readonly reports aggregate mismatch / unowned exposure | readiness FAIL until exchange/local/protection are resolved |
| grid reservation-owned stop fill recovery | protection sync uses desired state + exchange active orders; same symbol/side but different PID means different ledgers; same PID + same exchange fill unit = one ledger application; snapshot is projection; actual exchange fill must be recovered before correction flatten | grid PID has SHORT snapshot OPEN 14220 with ACTIVE `GRID_STOP`, Binance historical `GRID_STOP` close exists with userTrades, current exchange aggregate stays positive because another PID still owns same symbol/side | 1 `GRID_EXCHANGE_RECONCILED_EXIT_FILL` row with preserved `sourceTradeId` for the reservation-owned stop fill | reservation owner snapshot closes to `CLOSED/openQty=0` while the other PID snapshot remains open | grid short leg converges to idle/closed without copying aggregate qty from the other PID | matched reservation becomes `FILLED`, sibling reservation finalized, other PID reservations untouched | `GRID_RESERVATION_EXIT_RECOVERY_*` trace and duplicate ignored on replay | reservation-owned historical fill closes only the owner PID, sibling reservations finalize, duplicate replay ignored, no correction replaces the actual fill |
| six positions eight conditionals protection shortage | every PID-owned open exposure needs effective PID-owned TP/STOP protection | six local/Binance-owned exposures exist, but only eight active conditional protections exist because two grid LONG legs have no TP/STOP while another PID has same symbol/side protection | no writes | no writes | no writes | no writes | live-readonly flags `PID_OPEN_NO_EFFECTIVE_PROTECTION` / `PID_OPEN_PROTECTION_COUNT_BELOW_EXPECTED` for affected PIDs | aggregate symbol/side protection count cannot mask PID-level missing protection |
| grid duplicate old exit recovery does not cancel current protection | duplicate historical exit recovery must not cancel a new cycle's active protection | grid leg had an old GMANUAL fill already ledgered, then a new same-leg entry opens with active TP/STOP; truth sync observes the old GMANUAL again as duplicate | no new exit row; duplicate ignored | current snapshot remains OPEN | current grid leg remains OPEN with current qty | current TP/STOP remain ACTIVE; no sibling cancel on duplicate-only recovery | `GRID_RESERVATION_EXIT_RECOVERY_DUPLICATE_NO_SIBLING_CANCEL` | duplicate old exit recovery cannot call current `cancelGridOrders` or cancel current active protection |
| signal entry partially filled then canceled | PARTIALLY_FILLED is intermediate; canceled remainder is not exposure | signal entry partially fills qty A, later CANCELED with no more trades | one entry row for qty A only | snapshot OPEN qty A | signal runtime qty A | TP/STOP qty A if protection is possible; unfilled remainder not protected/counts as no exposure | partial/canceled state audit | local exposure and protection are based on executed qty only |
| grid entry partially filled then expired | PARTIALLY_FILLED is intermediate; expired remainder is not exposure | grid limit entry partially fills qty A, later EXPIRED | one entry row for qty A only | snapshot OPEN qty A | grid leg OPEN qty A | grid TP/STOP qty A; remaining entry qty expired | partial/expired state audit | no full origQty projection and no unprotected filled exposure |
| PARTIALLY_FILLED state-machine transition matrix | PARTIALLY_FILLED must never be terminal | WebSocket/REST state starts PARTIALLY_FILLED then stays partial, FILLED, CANCELED, EXPIRED, REJECTED, or temporarily missing from REST | apply only new tradeIds once | snapshot reflects cumulative executed qty only | runtime row reflects executed exposure or terminal state | protection follows actual filled exposure; unknown remainder stays auditable | UNKNOWN_PARTIAL_STATE / retry / USER_ACTION_REQUIRED when unprotected | all next states preserve filled units and never treat partial status as final |
| NEW then CANCELED unfilled | terminal unfilled orders create no exposure | order NEW, later CANCELED, executedQty=0, no userTrades | no ledger row | no OPEN snapshot | order state terminal canceled | no protection | `ORDER_TERMINAL_WITHOUT_FILL` | unfilled canceled remainder is not projected as position |
| NEW then EXPIRED unfilled | terminal unfilled orders create no exposure | order NEW, later EXPIRED, executedQty=0, no userTrades | no ledger row | no OPEN snapshot | order state terminal expired | no protection | `ORDER_TERMINAL_WITHOUT_FILL` | expired remainder is not projected as position |
| NEW then EXPIRED_IN_MATCH unfilled | EXPIRED_IN_MATCH must be explicitly checked for trades | order NEW, later EXPIRED_IN_MATCH, no userTrades | no ledger row | no OPEN snapshot | order state terminal expired_in_match | no protection | `ORDER_EXPIRED_IN_MATCH_NO_FILL` | userTrades checked and no false exposure from origQty |
| NEW then REJECTED | rejected order creates no exposure unless trades prove otherwise | order NEW, later REJECTED, no userTrades | no ledger row | no OPEN snapshot | order state terminal rejected/failed | no protection | `ORDER_REJECTED_NO_FILL` | no false exposure; failure remains auditable |
| PARTIALLY_FILLED then FILLED | partial status is intermediate and later full fill applies remaining tradeIds | partial trade A, later FILLED with trade B | one row per tradeId | snapshot openQty A+B | runtime qty A+B | TP/STOP qty A+B | `PROTECTION_SYNC_FOR_PARTIAL_EXPOSURE` | all tradeIds applied once and protection matches final executedQty |
| PARTIALLY_FILLED then CANCELED | canceled remainder is not exposure | partial trade A, later CANCELED, no more trades | ledger only A | snapshot OPEN qty A | runtime qty A | TP/STOP qty A | `ORDER_PARTIAL_REMAINDER_CANCELED` | origQty remainder is canceled and never counted |
| PARTIALLY_FILLED then EXPIRED | expired remainder is not exposure | partial trade A, later EXPIRED, no more trades | ledger only A | snapshot OPEN qty A | runtime qty A | TP/STOP qty A | `ORDER_PARTIAL_REMAINDER_EXPIRED` | origQty remainder is expired and never counted |
| PARTIALLY_FILLED then EXPIRED_IN_MATCH | EXPIRED_IN_MATCH after partial preserves fills and expires remainder | partial trade A, later EXPIRED_IN_MATCH | ledger only userTrades | snapshot OPEN filledQty | runtime qty filledQty | TP/STOP filledQty | `ORDER_EXPIRED_IN_MATCH_WITH_FILL` | filledQty remains protected; remainder is terminal expired-in-match |
| PARTIALLY_FILLED then REJECTED | rejected remainder does not erase existing fills | partial trade A, later REJECTED | ledger only existing trades | snapshot OPEN filledQty | runtime qty filledQty or blocked risk if unprotected | TP/STOP filledQty or live-readonly FAIL | `ORDER_REJECTED_WITH_FILL` | existing fill preserved; missing protection is USER_ACTION_REQUIRED |
| PARTIALLY_FILLED then PARTIALLY_FILLED again | repeated partial events apply only new tradeIds | partial trade A, later partial event repeats A and adds B | ledger A and B once | snapshot cumulative A+B | runtime qty cumulative executedQty | TP/STOP cumulative executedQty | `ORDER_PARTIAL_STATE_REST_CHECK` | duplicate tradeIds ignored; order remains non-terminal |
| exit order PARTIALLY_FILLED then CANCELED | partially canceled exit closes only executedQty | openQty Q, exit partial A, later CANCELED | exit ledger A only | snapshot OPEN Q-A | runtime remaining qty Q-A | remaining position keeps protection | `ORDER_PARTIAL_REMAINDER_CANCELED` | unfilled close remainder does not close the PID |
| grid entry PARTIALLY_FILLED then EXPIRED_IN_MATCH | grid leg exposure is executedQty, not origQty | grid entry partial A, later EXPIRED_IN_MATCH | grid entry ledger A | grid snapshot OPEN A | grid leg OPEN A | grid TP/STOP A | `ORDER_EXPIRED_IN_MATCH_WITH_FILL` | grid leg is not projected to full original order qty |
| signal entry PARTIALLY_FILLED then EXPIRED_IN_MATCH | signal exposure is executedQty, not origQty | signal entry partial A, later EXPIRED_IN_MATCH | signal entry ledger A | signal snapshot OPEN A | signal row EXACT/r_qty=A | signal TP/STOP A | `ORDER_EXPIRED_IN_MATCH_WITH_FILL` | signal row/protection follow executedQty only |
| protection order PARTIALLY_FILLED then EXPIRED_IN_MATCH | partial protection fill reduces only executed close qty | openQty Q, TP/STOP partial close A, later EXPIRED_IN_MATCH | exit ledger A | snapshot OPEN Q-A | runtime remaining qty Q-A | remaining qty protected or USER_ACTION_REQUIRED | `ORDER_EXPIRED_IN_MATCH_WITH_FILL` | remaining exposure is not hidden and over/under-protection is flagged |
| terminal status with late userTrade | late trade after terminal is recovered once | terminal CANCELED/EXPIRED/EXPIRED_IN_MATCH/REJECTED later yields userTrade | ledger late tradeId once | snapshot reflects late executedQty | runtime converges from recovered fill | protection follows recovered exposure | `ORDER_TERMINAL_WITH_EXECUTED_QTY` | late tradeId applied once; duplicate ignored |
| OFF payload is not delete intent | OFF and DELETE are separate control paths | frontend/backend OFF payload has `enabled=N` and no explicit delete confirmation | no ledger mutation | no snapshot mutation | row remains present with enabled=N/status auditable | no reservation mutation | `USER_OFF` audit, not delete audit | OFF payload fails delete-intent guard and cannot reach physical delete |
| strategy delete requires explicit USER_DELETE_STRATEGY intent | delete only occurs after explicit user delete action | delete request includes `confirmDelete=true` and `deleteIntent=USER_DELETE_STRATEGY` | no trading ledger mutation | no snapshot mutation | strategy row deletion may proceed only after readiness guard | no reservation mutation | `USER_DELETE_STRATEGY` audit before delete | delete endpoint rejects missing/wrong intent and deprecated `/admin/play/del` is disabled |
| OFF row remains visible in OFF tab | disabled rows are still strategy settings, not deleted rows | API row returns `enabled=N`, `controlState=OFF`, or raw string forms | no ledger mutation | no snapshot mutation | frontend parses raw `N/OFF/STOP` as disabled and keeps row in OFF list | no reservation mutation | none required | UI state updates in place; OFF does not remove row from local list except tab movement |
| order terminal display state for admin console | terminal/intermediate order statuses must be visible to operators | runtime event has status `PARTIALLY_FILLED`, `CANCELED`, `EXPIRED`, `EXPIRED_IN_MATCH`, or `REJECTED` plus executed/orig qty | no writes | no writes | admin payload exposes `orderDisplayState`, `riskStatus`, `remainingQty`, `systemAction`, `nextUserAction` | no writes | display helper labels partial/terminal state | admin can see executedQty, remainingQty, error reason, and system/user action without reading raw logs |
| admin expected-ignore lifecycle classification | expected no-op events must not inflate abnormal order counts | runtime/admin event is `NO_MATCHING_STRATEGY`, `GRID_ACTIVE_IGNORED`, duplicate fill ignored, smoke unmatched webhook, or reset skipped already ready | no writes | no writes | admin payload exposes `lifecycleResult=EXPECTED`, `severity=INFO`, `expectedOrAbnormal=EXPECTED` | no writes | expected event label remains informational | Operations Console does not mark expected-ignore lifecycle as abnormal |
| QA cleanup refuses production PID without QA marker | QA cleanup must be marker-scoped and never PID-range scoped | cleanup requested for production-like PID or missing PID without `QA_` strategy row marker | no ledger/reservation/msg deletion | no snapshot deletion | no strategy row deletion | no reservation deletion | cleanup result includes `blockedPids` and `guard=QA_MARKER_REQUIRED` | cleanup cannot delete `live_play_list`/`live_grid_strategy_list` rows or artifacts by numeric PID alone |
| production signal PID deleted by QA cleanup numeric collision | QA cleanup must never delete production signal rows that collide numerically with QA grid rows | production SQZGBRK PID has no QA marker and QA cleanup is invoked with the same numeric PID from a grid replay | no ledger deletion | no snapshot deletion | production `live_play_list` row remains | no reservation deletion | `QA_MARKER_REQUIRED` guard and blocked PID evidence | numeric PID alone is never a cleanup authority |
| controlled restore of QA-cleanup-deleted production signal PID | restore must be application-controlled, whitelisted, and disabled-safe | target PID row is physically absent, no `USER_DELETE_STRATEGY` audit, clean Binance/local gate, high-confidence source row exists | no ledger mutation | no snapshot mutation | row restored as `enabled=N/status=READY/r_qty=0` | no reservation mutation | `CONTROLLED_RESTORE_QA_CLEANUP` audit with source evidence | only PID 991744-991753 strategy config rows are inserted and user API shows OFF rows |
| restore blocked for low confidence PID | restore must fail closed when exact source values are not proven | target PID is missing but source row/audit/cleanup collision evidence is incomplete | no ledger mutation | no snapshot mutation | no strategy row insert | no reservation mutation | `RESTORE_BLOCKED_LOW_CONFIDENCE` report row | low/medium confidence candidates are not restored automatically |
| restored PID appears in user OFF tab | restored disabled strategy rows remain visible as settings | restored SQZGBRK row has `enabled=N`, `status=READY`, and `r_qty=0` | no writes | no writes | user API returns row and UI groups it as OFF/대기중 | no writes | restore history visible in admin | user table shows restored row without marking it deleted |
| admin physical missing PID without delete audit is critical | physical absence without user delete is an operator risk | audit history exists for PID but strategy row is absent and no `USER_DELETE_STRATEGY` audit exists | no writes | no writes | admin derived state is restore-required | no writes | `restoreStatus=RESTORE_REQUIRED`, severity CRITICAL | Operations Console surfaces controlled restore requirement |
| admin restored PID is no longer active abnormal | completed controlled restore is audit history, not an active abnormal | PID row exists after controlled restore, disabled and flat | no writes | no writes | admin derived state is RESTORED/OFF/READY | no writes | restore audit retained | active abnormal count excludes restored safe rows |
| system reset skipped disabled row is not abnormal | disabled rows should not emit noisy abnormal reset results | `SYSTEM_RESET_READY_SKIPPED_DISABLED` or already-ready reset is emitted | no writes | no writes | lifecycleResult EXPECTED/INFO | no writes | expected-ignore classification | repeated disabled reset skips do not inflate abnormal cards |
| cleanup final sweep cannot delete production rows | final sweep obeys the same QA marker guard | replay final cleanup sees production uid=147 row with matching numeric PID but no QA marker | no deletion | no deletion | row remains | no deletion | blocked cleanup audit/report | final sweep cannot delete production rows by id range |
| delete requires explicit USER_DELETE_STRATEGY | OFF/delete UI paths stay separated | delete request lacks `confirmDelete=true` or `deleteIntent=USER_DELETE_STRATEGY` | no trading mutation | no snapshot mutation | row remains | no reservation mutation | delete request rejected and no delete audit written | only explicit delete button with intent can remove a strategy row |
| grid external manual close recovery with attributable fill | user closes Binance first; actual fill attribution is preferred over correction | grid PID LONG/SHORT snapshot OPEN, Binance is flat, no active protection, one `web_*` MARKET reduceOnly close qty exactly matches the single local owner | 1 `GRID_EXTERNAL_MANUAL_CLOSE_FILL` row with preserved source order/trade/clientOrderId | snapshot closes to `CLOSED/openQty=0` | grid leg converges to idle/closed and qty 0 | no new protection; no correction replaces the actual fill | `GRID_EXTERNAL_CLOSE_RECOVERY_*` trace | single-owner web close is recovered as actual exit fill and duplicate observation does not double close |
| grid external manual close correction fallback | correction is allowed only under strict safe gate when actual fill cannot be attributed | grid PID OPEN, Binance side flat, no active Binance/local protection, no recoverable close trade, exactly one local owner | explicit `GRID_EXCHANGE_FLAT_LOCAL_STALE_FLATTEN` correction row with realizedPnl 0 | snapshot closes | grid leg idles | no reservation/protection mutation needed | `GRID_EXCHANGE_FLAT_RECONCILE` trace | fallback is explicit and only used after attribution fails safely |
| grid external manual close ambiguous multi-PID | same symbol/side ambiguity must not silently flatten one owner | two local grid/signal owners share symbol/side, Binance flat, manual close cannot be attributed to one PID | no new exit/correction row for the ambiguous PID | snapshots remain OPEN | rows are not hidden as flat | no local reservation cancellation and no Binance cancel path | `GRID_EXTERNAL_CLOSE_CORRECTION_FLATTEN_BLOCKED` with owner ambiguity | readiness blocks with USER_ACTION_REQUIRED/BLOCKED_BY_AMBIGUOUS_EXTERNAL_CLOSE |
| signal external manual close then user OFF | signal and grid must converge consistently when user closes on Binance before UI OFF | signal PID OPEN/EXACT, Binance side flat, no active protection/order, strategy disabled after user OFF | actual fill recovery if attributable, otherwise explicit safe flat convergence | snapshot closes | signal row READY/r_qty=0 | no orphan protection and no duplicate recovery | truth-sync exchange-flat audit | signal ends READY without issuing a new close order |
| grid external close with active orphan protection | active protection after external close is unsafe and must not be hidden | Binance position flat, grid local OPEN, active grid TP/STOP reservation/order remains | no local close/correction row | snapshot remains OPEN/stale until user resolves protection | row remains auditable | active reservation is not silently CANCELED | `GRID_EXTERNAL_CLOSE_CORRECTION_FLATTEN_BLOCKED` active-reservation audit | USER_ACTION_REQUIRED; Codex/runtime must not silently flatten over active protection |
| grid webhook timeframe alias normalization | canonical grid timeframe aliases must exact-match one stored bunbong without duplicate candidate | stored grid PID bunbong uses `1H`, `2H`, `30MIN`, `1MIN`, `5MIN`; webhook payload sends `60`, `60MIN`, `1H`, `120`, `2H`, `30`, `1`, `5` | none; dry-run only | none; dry-run only | exact candidate selection uses canonical bunbong (`1H`, `2H`, `30MIN`, `1MIN`, `5MIN`) | none; dry-run only | dry-run trace/report only | `60/60MIN/1H -> 1H`, `120/2H -> 2H`, `30 -> 30MIN`, `1 -> 1MIN`, `5 -> 5MIN`, and each payload resolves to exactly one expected PID |
| signal strategy alias internal code mapping | display strategy name and runtime identity are separate; webhook alias maps to the stored internal code | displayName `SQZ+GRID+BREAKOUT`, strategyCode `SQZGBRK`, stored `live_play_list.type=SQZGBRK`, webhook `db_type=SQZ+GRID+BREAKOUT`, symbol `PUMPUSDT.P`, timeframe `15MIN` | none; dry-run matching only | none; dry-run matching only | create path uses internal code and default disabled expectation; signal route candidate lookup matches `SQZGBRK` | none; dry-run only | dry-run trace/report only | no truncation, create constraint allows `SQZGBRK`, webhook alias finds the SQZGBRK PID, lowercase aliases work, existing `ATF+VIXFIX`/`NP_ATF+VIXFIX` matching remains unaffected |
| signal force-OFF with runtime READY but snapshot OPEN | runtime READY alone cannot skip close when authoritative snapshot is OPEN | signal PID OFF request, legacy status `EXACT`, `decorateSignalItemSync(item)` without snapshot would be `READY`, but `live_pid_position_snapshot` is `OPEN/openQty>0` | close path is scheduled before any protection cleanup | snapshot remains OPEN until close fill; no false READY/CLOSED | signal row stays `EXACT` while close is pending | active protection cancel is deferred before close attempt | `OFF_REQUEST_RECEIVED`, `OFF_CLOSE_REQUIRED_DETECTED`, `OFF_PROTECTION_CANCEL_DEFERRED` | closeRequired=true due to snapshot OPEN, close scheduling cannot be skipped by runtime READY |
| signal force-OFF close failure does not remove protection silently | failed close cannot leave exchange-open/no-protection without USER_ACTION_REQUIRED | signal PID open with active protection, close attempt rejected or not attempted | no false exit ledger | snapshot remains OPEN | row is not silently reset to READY/CLOSED | protection remains active if not canceled yet; if already canceled, USER_ACTION_REQUIRED is emitted | `OFF_CLOSE_FAILED_POSITION_STILL_OPEN` or `OFF_USER_ACTION_REQUIRED` | failed close does not silently remove protection or hide the risk |
| signal force-OFF normal close | protection cancellation occurs after close fill or exchange flat confirmation | signal PID open with active protection, close order accepted and filled | close fill ledger row, openQtyAfter=0 | snapshot CLOSED/openQty=0 | signal row converges after close | protection cleanup runs after close | `OFF_PROTECTION_CANCEL_AFTER_CLOSE` | close lifecycle completes before protection is removed |
| live read-only detects unprotected open position | open local/exchange position with zero local/exchange protection is USER_ACTION_REQUIRED | Binance position exists, local snapshot OPEN, local active reservation count=0, Binance active protection count=0 | no writes | no writes | no writes | no writes | read-only report flags `EXCHANGE_OPEN_NO_PROTECTION` | aggregate comparison may pass, but protection risk check fails readiness |
| local canceled reservation but Binance active order | local reservation cannot be considered canceled unless Binance cancel/fill/expire is confirmed | local reservation status=`CANCELED`, Binance openAlgoOrder active with same clientOrderId | no writes | no writes | no writes | no writes | read-only report flags `LOCAL_CANCELED_BUT_BINANCE_ACTIVE` / `BINANCE_ONLY_ACTIVE_PROTECTION` | readiness FAIL; no local mutation |
| Binance position open but local snapshot flat | snapshot is projection and must not hide exchange exposure | Binance position qty > 0, local openQty sum = 0 | no writes | no writes | no writes | no writes | aggregate report flags `BINANCE_OPEN_LOCAL_FLAT` | readiness FAIL / USER_ACTION_REQUIRED |
| Binance flat side has active close order | protection sync uses exchange active orders even when local reservations are empty | Binance position side qty=0, active reduceOnly close order remains | no writes | no writes | no writes | no writes | read-only report flags `ORPHAN_CLOSE_ORDER_FOR_FLAT_SIDE` | readiness FAIL or explicit USER_ACTION_REQUIRED |
| oversized Binance protection vs aggregate position | protection qty must not materially exceed exchange aggregate or PID-owned qty | Binance position qty is tiny/dust, active reduceOnly close order qty is much larger | no writes | no writes | no writes | no writes | read-only report flags `OVERSIZED_PROTECTION_VS_POSITION` | readiness FAIL; Codex does not cancel the order |
| position shrink with stale full-size protection | protection qty must resize or be surfaced after partial close/shrink | PID or exchange position shrinks but active TP/STOP remains at original larger qty | no writes | no writes | no writes | local reservation remains auditable but unsafe | live-readonly flags `OVERSIZED_PROTECTION_VS_POSITION` / `OVERSIZED_PROTECTION_VS_PID_OPEN_QTY` | readiness FAIL; no silent PASS from matched reservation alone |
| unknown exchange mutation forensic | exchange close source must be proven by allOrders/userTrades/clientOrderId | reduceOnly close trade appears after user denies manual action | close order/trade is classified by prefix/source | local projection compared to fill unit evidence | row state is not assumed safe | reservation/protection aftermath surfaced | `GMANUAL_*` maps to platform/runtime/script, `web_*` maps to Binance web/manual | unknown source blocks Live QA until resolved |
| runtime outgoing Binance write audit required | QA replay and QA temp strategies must never write to Binance | QA replay/temp PID attempts grid market close | no Binance write; return null before `futuresOrder` | no exchange mutation | row remains local QA artifact only | no exchange reservation created | blocked write trace/audit with callsite | data replay cannot shrink real live positions |
| QA replay live-write escape containment | data replay must fail closed before any Binance POST/DELETE/order/close path | run-all-data-replay or targeted replay invokes GMANUAL grid close, signal force-OFF close, TP/STOP create, or cancel path using uid=147-style scenario data | no Binance ledger from exchange mutation; replay may only mutate QA local fixtures | no exchange mutation and no local production state mutation | QA temp rows only | no Binance protection/order created | `BINANCE_WRITE_BLOCKED_BY_GUARD` / `QA_REPLAY_MODE_BINANCE_WRITE_BLOCKED`; before/after allOrders/userTrades delta 0 | scenario PASS only when blocked before Binance client write and exchange deltas remain zero |
| QA replay network firewall blocks Binance writes | centralized guard bypass is still contained by network method firewall | replay process attempts signed POST/DELETE/PUT/PATCH to Binance host directly or through qa-binance | no writes | no exchange mutation | no row mutation beyond scenario-local audit | no reservation/order mutation | `QA_REPLAY_BINANCE_NETWORK_WRITE_BLOCKED` | Binance GET read-only remains allowed, but all write methods throw in replay mode |
| production UID/API key in data replay fails closed | replay cannot use production credentials for write-capable client initialization | data replay resolves uid=147 or other real credential account while `QA_REPLAY_MODE=1` | no exchange write | no exchange mutation | fixture client can be injected explicitly; real credential client is not initialized | no reservation/order mutation | `QA_REPLAY_MODE_BINANCE_CLIENT_BLOCKED` | replay uses fixture/mock clients for exchange recovery tests and never creates a real write-capable Binance client |
| exact accidental GMANUAL replay is blocked | incident clientOrderId pattern must be reproduced as a guard-blocked replay | scenario PID/clientOrderId shaped like `GMANUAL_L_147_991914_*` calls `closeGridLegMarketOrder` | no Binance close fill | no exchange mutation | grid temp row remains local-only | no exchange reservation/order mutation | blocked grid market close log with caller/action/symbol/clientOrderId | no allOrders/userTrades/openOrders/openAlgoOrders delta and scenario PASS only if blocked |
| incident impact stays separated from user performance surfaces | QA-induced exchange fills must be auditable but not silently treated as normal user performance | real Binance history contains QA_REPLAY_ACCIDENT tradeIds but local ledger/msg/runtime rows do not classify them as normal strategy fills | no raw DB patch | display/aggregation excludes or labels QA_REPLAY_ACCIDENT if surfaced from Binance history | user Track Record/Revenue must not count accident as normal strategy outcome | no reservation mutation | admin incident report keeps exact orderId/tradeId/PnL/commission | raw history is preserved; any correction/exclusion beyond display requires explicit approval |
| cross-PID overfill guard with TP/GMANUAL | close/TP/GMANUAL fill larger than PID-owned qty must be audited and not silently close other PID ownership | PID owns qty=10, incoming owner-prefixed exit fill qty=15 while aggregate may include other PIDs | ledger applies only PID-owned qty and records audit evidence | owner snapshot closes only its qty | other PID is not touched | reservation overfill is flagged | `FILL_QTY_EXCEEDS_PID_OWNED_QTY` and `FILL_QTY_EXCEEDS_RESERVATION_QTY` | no silent overfill; USER_ACTION_REQUIRED audit is present |
| cross-PID same-symbol same-side close quantity guard | same symbol/side but different PID means different ledgers; close qty must be bounded by PID-owned qty | PID A signal LONG openQty 13938, PID B grid LONG openQty 13950, same account/symbol/side, PID B BOX_BREAK close triggered | PID B close reservation/order uses `finalCloseQty <= 13950`; PID A ledger untouched | PID A snapshot remains OPEN, PID B close intent limited to its own qty | PID A runtime row stays intact; PID B records its own close attempt only | PID A protection remains intact until its own exit; PID B reservation uses owned qty only | `PID_CLOSE_QTY_GUARD` and aggregate mismatch trace when blocked | no cross-PID over-close and no PID A flatten caused by PID B close |
| exit attribution by clientOrderId/reservation ownership | same symbol/side but different PID means different ledgers; exit attribution follows reservation/clientOrderId ownership | reduceOnly close order clientOrderId belongs to PID B while Binance aggregate contains PID A + PID B | PID B exit attributed only up to PID B owned qty | PID A snapshot not closed by PID B clientOrderId | PID A runtime row remains open until its own exit | PID B reservation/clientOrderId ownership remains primary attribution key | ownership-aware audit trace | PID B close does not close PID A ledger; aggregate mismatch is reported, not hidden |
| exchange flat / local OPEN with recovered close | flat exchange + recovered close must converge local state | exchange qty 0, local OPEN, recovered close exists | exit recovery plus optional correction | CLOSED, openQty 0 | grid IDLE or signal READY | protection cleared | reconcile close audit | stale OPEN removed without pnl duplication |
| exchange flat / local OPEN without recovered close | flat exchange + no protection + local OPEN must flatten | exchange qty 0, local OPEN, no recovered close | correction event only | CLOSED, openQty 0 | grid IDLE or signal READY | protection cleared | local stale flatten audit | stale loop ends, correction auditable |
| orphan flatten | no silent delete, correction only | local stale qty remains but exchange has no position | orphan close correction row | CLOSED, openQty 0 | runtime row converged | reservations cleared if needed | orphan close audit | history preserved, qty flattened |
| correction event PnL | correction event must not double pnl | exit pnl already exists, later correction flattens qty | correction realizedPnl remains 0 | cycle pnl unchanged | projection converged | no extra reservation side effect | correction audit | summed realizedPnl unchanged |
| live read-only aggregate comparison | exchange aggregate and PID local sum are different truth layers but must reconcile at aggregate level | real UID, Binance read-only, local DB snapshots | no writes | no writes | no writes | no writes | read-only comparison output | aggregate gap is reported, not patched |
| live read-only protection comparison | protection truth uses exchange active orders plus local ownership | real UID, Binance open orders/open algo + local reservations | no writes | no writes | no writes | comparison only | mismatch table and stale flags | mismatches surfaced without mutation |
| live execution preflight for signal | live execution must fail closed unless target is explicitly safe | real UID, signal strategy config, no execution | no writes | no writes | signal row only read | reservation only read | preflight table | any fail blocks execution |
| live execution preflight for split TP | split TP config must match intended structure before live execution | real UID, signal strategy with split TP | no writes | no writes | signal row read only | reservation read only | preflight table | split TP config matches expected values |
| live execution preflight for grid | grid QA target must not collide with live state | real UID, grid strategy target | no writes | no writes | grid row read only | reservation read only | preflight table | no position/order contamination and config matches |
| user trading table is profit-first | user trading page must emphasize PnL/performance instead of internal order lifecycle | user opens Trading page after clean gate/restored rows | no writes | no writes | signal rows remain visible, including OFF restored rows | no reservation mutation | performance summary cards show realized/period PnL and unavailable data is not faked | obsolete regime/quote/order-setting widgets are hidden from the top-level view |
| restored SQZGBRK rows visible as OFF | controlled restored production signal rows are disabled settings, not deleted rows | PID 991744-991753 restored with enabled=N/status READY/r_qty=0 | no writes | no writes | user API/UI shows rows in OFF/all strategy table | no reservation mutation | `userStatusLabel=대기중` and OFF tab contains rows | no accidental ON and delete still requires explicit confirm intent |
| user messages only show Binance-origin actionable issues | platform/dev/internal logs must not become user alerts | msg/runtime rows contain ngrok/smoke/replay/no-match/duplicate plus Binance rejection/protection rows | no writes | no writes | message API returns only actionable user-facing rows | no reservation mutation | hidden reason for internal rows, BINANCE category for visible rows | no ngrok/smoke/replay/internal reset text appears in user message modal |
| My Page readiness flags futures balance missing | user needs actionable auto-trading readiness, not vague risk terms | total wallet may exist but futures available USDT is 0 or missing | no writes | no writes | account readiness response is ACTION_REQUIRED/BLOCKED | no writes | issue code `FUTURES_BALANCE_USDT_EMPTY` when available balance is 0 | copy instructs Binance spot-to-futures USDT transfer before turning on strategies |
| track record excludes expected-ignore events from abnormal count | Track Record is performance history, not operational noise | order process rows include NO_MATCHING_STRATEGY, GRID_ACTIVE_IGNORED, duplicate ignored, smoke unmatched | no writes | no writes | completed/open/review tabs separate expected-ignore from review | no writes | expected-ignore rows are INFO/EXPECTED and not prominent abnormal records | default tab is completed performance with realizedPnl/result/return columns |
| track record completed trade uses fill units | performance record must be tied to ledger fill units | orderId has multiple sourceTradeIds and realizedPnl rows | ledger read only | snapshot read only | track record aggregates completed cycle without double counting expected-ignore | reservation read only | source is `live-ledger` or explicitly legacy-estimated | multiple tradeIds can contribute to one series while preserving fill-unit accounting |
| revenue uses tradeId/sourceTradeId not order count | revenue must be fill-based, not order-count based | Binance runtime event log contains ORDER_TRADE_UPDATE TRADE rows, potentially multiple tradeIds under one orderId | no writes | no writes | revenue summary reports tradeCount and notional from deduped fill events | no writes | source says `binance-runtime-event-log-trade-units` | total notional/fee/estimated revenue are labeled as actual/estimated with data availability |
| admin expected-ignore is INFO not abnormal | Operations Console abnormal cards must exclude expected no-ops | expected-ignore lifecycle rows are present with INFO/EXPECTED | no writes | no writes | admin summary cards count WARN/CRITICAL, not expected INFO | no writes | severity split remains visible | expected-ignore does not increase abnormal cards while true critical states remain critical |
| admin critical state remains critical | user-friendly filtering must not hide real trading risk from admin | open without protection, Binance/local mismatch, terminal with fill no ledger, missing physical row | no writes | no writes | admin lifecycle classifier returns CRITICAL/ABNORMAL | no writes | nextAction/systemAction is visible | admin console still surfaces canonical risk even if user UI stays simple |
